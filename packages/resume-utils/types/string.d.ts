/**
 * 去除字符串中的所有空白字符（包括空格、制表符、换行符等）
 * @param str 需要处理的字符串
 * @returns 去除空白字符后的字符串
 */
export declare function removeWhitespace(str: string): string;
/**
 * 去除字符串中的所有空格
 * @param str 需要处理的字符串
 * @returns 去除空格后的字符串
 */
export declare function removeSpaces(str: string): string;
/**
 * 去除字符串中的所有换行符
 * @param str 需要处理的字符串
 * @returns 去除换行符后的字符串
 */
export declare function removeLineBreaks(str: string): string;
/**
 * 压缩字符串中的连续空白字符为单个空格
 * @param str 需要处理的字符串
 * @returns 压缩空白后的字符串
 */
export declare function compressWhitespace(str: string): string;
